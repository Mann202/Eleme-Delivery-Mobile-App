<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/primary"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".Login.ConfirmEmail">

    <ImageView
        android:id="@+id/logo_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher_background"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/rounded_background"
        android:paddingStart="25dp"
        android:paddingTop="30dp"
        android:paddingBottom="60dp"
        android:paddingEnd="25dp">

        <TextView
            android:id="@+id/sub_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/lato_bold"
            android:gravity="start"
            android:text="@string/msg_confirm_your_email"
            android:textColor="@color/lightdark"
            android:textFontWeight="900"
            android:textSize="40dp" />

        <TextView
            android:id="@+id/descripsion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/lato_regular"
            android:textColor="@color/darkbg"
            android:textSize="20sp"
            android:layout_marginTop="15dp"
            android:text="@string/msg_please_check_your_inbox_for_a_confirmation_email_click_the_link_in_the_email_to_confirm_your_email_address"
            android:layout_below="@+id/sub_title"/>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/getstartedbtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="25dp"
            android:text="@string/msg_re_send_confirmation_email"
            android:layout_below="@+id/descripsion"
            style="@style/PrimaryButton"/>
    </RelativeLayout>
</RelativeLayout>
